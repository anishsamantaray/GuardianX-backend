version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - cd terraform/
      - terraform init
      - terraform apply -auto-approve -var-file=../image.auto.tfvars
